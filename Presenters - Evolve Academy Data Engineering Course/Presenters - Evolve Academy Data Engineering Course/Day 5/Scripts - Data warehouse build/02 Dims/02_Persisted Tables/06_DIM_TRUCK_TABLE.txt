CREATE OR REPLACE TABLE DATA_WAREHOUSE.PUBLIC.DIM_TRUCK
(
TRUCK_SK INT,
TRUCK_ID INT,
REGION VARCHAR(256),
COUNTRY VARCHAR(256),
YEAR INT,
MAKE VARCHAR(256),
MODEL VARCHAR(256),
TRUCK_BRAND_NAME VARCHAR(256)
);