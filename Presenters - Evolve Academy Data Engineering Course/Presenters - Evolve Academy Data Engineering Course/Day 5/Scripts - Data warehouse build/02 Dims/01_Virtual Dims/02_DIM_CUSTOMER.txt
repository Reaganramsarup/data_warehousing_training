CREATE VIEW DATA_WAREHOUSE.PUBLIC.VIRTUAL_DIM_CUSTOMER
AS 
SELECT 
CUSTOMER_ID,
FIRST_NAME,
LAST_NAME,
CONCAT(FIRST_NAME, ' ', LAST_NAME) as FULL_NAME,
GENDER,
BIRTHDAY_DATE,
DATEDIFF(YEAR, BIRTHDAY_DATE, CURRENT_DATE) AS AGE,
E_MAIL,
PHONE_NUMBER
FROM HOL_DB.RAW_CUSTOMER.CUSTOMER_LOYALTY